<UserControl x:Class="PersonellRecords.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonellRecords.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <!-- текст -->
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <!-- фон -->
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#4c575e" Offset="0.0"/>
                        <GradientStop Color="White" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <!-- обводка -->
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image Source="/Resources/bg/bgLogin.jpg" Grid.ColumnSpan="3" Grid.RowSpan="6" Stretch="UniformToFill"/>
        <Border MinWidth="400" MaxHeight="300" Background="White" HorizontalAlignment="Center" Padding="40" CornerRadius="30" Grid.Column="0"  Grid.Row="0" Grid.RowSpan="3" Grid.ColumnSpan="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="50" Color="#2E3446" ShadowDepth="30"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Авторизация" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Margin="0 0 0 20"/>
                <Grid>
                    <Button Name="ButInput" Content="Вход" HorizontalAlignment="Left" Style="{StaticResource ButtonStyle}" Click="ButInput_Click"/>
                    <Button Name="ButRegister" Content="Регистрация" HorizontalAlignment="Left" Margin="220,0,0,0" Style="{StaticResource ButtonStyle}" Height="30" VerticalAlignment="Top" Click="ButRegister_Click"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Логин" Margin="0 10 0 0" FontWeight="Bold"/>
                        <Image Source="/Resources/icons/login.png" Height="20" Width="20" Margin="7 10 0 0"/>
                    </StackPanel>
                    <TextBox x:Name="TBAuthor" Grid.Column="1" Margin="0 10 0 0" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.ColumnSpan="3">
                        <Label Content="Пароль" Margin="0 10 0 0" FontWeight="Bold"/>
                        <Image Source="/Resources/icons/pass.png" Height="20" Width="20" Margin="0 10 0 0"/>
                    </StackPanel>
                    <PasswordBox x:Name="PBPassword" Grid.Column="1" Margin="0,10,0,0"/>
                </Grid>
                <Button Name="BtnCloseProg" Content="Выйти из программы" Width="200" Margin="0 10 0 0" Style="{StaticResource ButtonStyle}" Click="BtnCloseProg_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

