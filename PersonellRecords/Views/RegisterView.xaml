<UserControl x:Class="PersonellRecords.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonellRecords.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <!-- текст -->
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <!-- фон -->
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#c5a709" Offset="0.0"/>
                        <GradientStop Color="#d9c473" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <!-- обводка -->
            <Setter Property="BorderBrush" Value="#906d05"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image Source="/Resources/bg/bgRegistration.jpg" Grid.ColumnSpan="3" Grid.RowSpan="6" Stretch="UniformToFill"/>
        <Border MinWidth="400" MaxHeight="300" Background="White" HorizontalAlignment="Center" Padding="40" CornerRadius="30" Grid.Column="0"  Grid.Row="0" Grid.RowSpan="4" Grid.ColumnSpan="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="50" Color="#2E3446" ShadowDepth="30"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Регистрация" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Margin="0 0 0 5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Логин" FontWeight="Bold"/>
                        <Image Source="/Resources/icons/login.png" Height="20" Width="20" Margin="8 0 0 0"/>
                    </StackPanel>
                    <TextBox x:Name="TBLogin" Grid.Column="1" />
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                        <Label Content="ФИО" FontWeight="Bold"/>
                        <Image Source="/Resources/icons/fio.jpg" Height="20" Width="20" Margin="15 0 0 0"/>
                    </StackPanel>
                    <TextBox x:Name="TBfio" Grid.Column="1" Margin="0 10 0 0"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                        <Label Content="Пароль" FontWeight="Bold"/>
                        <Image Source="/Resources/icons/pass.png" Height="20" Width="20"/>
                    </StackPanel>
                    <TextBox x:Name="TBPassword" Grid.Column="1" Margin="0 10 0 0"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                        <Label Content="Почта"  FontWeight="Bold"/>
                        <Image Source="/Resources/icons/mail.jpg" Height="20" Width="35"/>
                    </StackPanel>
                    <TextBox x:Name="TBEmail" Grid.Column="1" Margin="0 10 0 0"/>
                </Grid>
                <Grid>
                    <Button Name="ButReg" Content="Регистрация" HorizontalAlignment="Left" Margin="0 10 0 0" Style="{StaticResource ButtonStyle}" Click="ButReg_Click"/>
                    <Button Name="ButBack" Content="Отмена" HorizontalAlignment="Right" Margin="0 10 0 0" Style="{StaticResource ButtonStyle}" Height="30" VerticalAlignment="Top" Click="ButBack_Click"/>
                </Grid>
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="3" VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Label Content="*регистрация-переход в меню входа с сохранением данных" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Label Content="*отмена-переход в меню входа без сохранения данных" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>

