<UserControl x:Class="PersonellRecords.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonellRecords.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="30"/>
            <!-- текст -->
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <!-- фон -->
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#1a4ab0" Offset="0.0"/>
                        <GradientStop Color="#94cbc5" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <!-- обводка -->
            <Setter Property="BorderBrush" Value="#07073b"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image Source="/Resources/bg/bgMenu.jpg" Grid.ColumnSpan="3" Grid.RowSpan="6" Stretch="UniformToFill"/>
        <Border MinWidth="400" MaxHeight="300" Background="White" HorizontalAlignment="Center" Padding="40" CornerRadius="30" Grid.Column="0"  Grid.Row="1" Grid.RowSpan="4" Grid.ColumnSpan="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="50" Color="#2E3446" ShadowDepth="30"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Учёт персонала" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Margin="0 0 0 20"/>
                <Button Name="BtnWorkers" Content="Работники"  Margin="10 0 10 10" Style="{StaticResource ButtonStyle}" Click="BtnWorkers_Click"/>
                <Button Name="BtnVacancy" Content="Вакансии" Margin="10 0 10 10" Style="{StaticResource ButtonStyle}" Click="BtnVacancy_Click"/>
                <Button Name="BtnState" Content="Расписание" Margin="10 0 10 10" Style="{StaticResource ButtonStyle}" Click="BtnState_Click"/>
                <Button Name="BtnClose" Content="Выйти из программы" Margin="10 0 10 10" Style="{StaticResource ButtonStyle}" Click="BtnClose_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
