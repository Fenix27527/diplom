<Window x:Class="PersonelRecords.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonelRecords.Views"
        mc:Ignorable="d"
        Title="HistoryWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Source="/Resources/bg/bgRegistration.jpg" Grid.ColumnSpan="2" Stretch="UniformToFill"/>

        <!-- ✅ ЗАМЕНИЛИ ListBox на DataGrid -->
        <DataGrid x:Name="DataGridHistory" 
                  Grid.Column="0" 
                  Margin="0,0,10,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="DataGridHistory_SelectionChanged"
                  Opacity="0.7" Background="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                <DataGridTextColumn Header="№Записи" Binding="{Binding [№Записи]}" Width="Auto"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Тип}" Width="Auto"/>
                <DataGridTextColumn Header="Дата/Время" Binding="{Binding [Дата/Время]}" Width="Auto"/>
                <DataGridTextColumn Header="Изменил" Binding="{Binding Изменил}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="До изменения:" FontWeight="Bold" FontSize="16" Margin="0,15,0,5"/>
            <TextBlock Grid.Row="1" Name="OldValuesTextBlock" Text="(нет данных)" TextWrapping="Wrap" Margin="0,0,0,15" Background="#F0F0F0" Padding="5"/>
            <TextBlock Grid.Row="2" Text="После изменения:" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="3" Name="NewValuesTextBlock" Text="(нет данных)" TextWrapping="Wrap" Margin="0,0,0,15" Background="#F0F0F0" Padding="5"/>
            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <TextBlock Text="Тип операции: " FontWeight="Bold" FontSize="16"/>
                <TextBlock x:Name="TBType" FontWeight="Bold" FontSize="16" Text="—"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>